{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "clean": "rimraf .spago && rimraf output && rimraf .psa-stash",
    "build": "spago build",
    "build-examples": "./build-examples.sh",
    "open-examples": "open resources/*.html"
  },
  "devDependencies": {
    "purescript": "0.12.3",
    "purescript-psa": "0.7.3",
    "spago": "0.7.5"
  },
  "dependencies": {}
}
